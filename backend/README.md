# Backend

Node.js + Express + Socket.IO + PostgreSQL + Redis starter template.

## Setup

```bash
cp .env.example .env
# Edit .env with your database and redis credentials
npm install
```

## Run

```bash
# Development
npm run dev

# Production
npm start
```

## Logs

Logs are written to `logs/combined.log` and `logs/error.log`.
